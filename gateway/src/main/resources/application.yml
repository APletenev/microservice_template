server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: userdetails
          uri: http://127.0.0.1:8090
          predicates:
            - Path=/api/userdetails/**
          filters:
            - TokenRelay=
            - RemoveRequestHeader=Cookie

  security:
    oauth2:
      client:
        registration:
          gateway:
            provider: spring
            client-id: gateway
            client-secret: 6:hR*sd.53U)
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope: openid
        provider:
          spring:
            issuer-uri: http://localhost:9000